spring:
  application:
    name: shoppingInvoices-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      username: root
      password: s3cr3t

  boot:
    admin:
      client:
        url: http://localhost:8086